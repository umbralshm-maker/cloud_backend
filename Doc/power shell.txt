Falta esto (crítico):

👉 Tu sistema local TAMBIÉN debe tener esa variable

Windows (PowerShell)
setx SHM_API_KEY shm_local_desktop_2026_prod


Luego cierra y vuelve a abrir la terminal / PyCharm / VSCode.

Verificación rápida
echo $env:SHM_API_KEY